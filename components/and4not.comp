component and4not "Four-input AND NOT gate";
pin in bit in0;
pin in bit in1;
pin in bit in2;
pin in bit in3;
pin out bit out;
function _ nofp;
license "GPL";
;;
FUNCTION(_) {
out = !(in0 && in1 && in2 && in3);
}
